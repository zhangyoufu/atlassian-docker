on:
  push:

jobs:
  sync:
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - name: Serialize workflow runs
      if: github.ref == 'refs/heads/master'
      uses: softprops/turnstyle@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  build:
    needs: sync
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
# DONT-CHECKSUM-BEGIN
        - application: jira-software
          version: '8.13.0-EAP03'
          url: 'https://www.atlassian.com/software/jira/downloads/binary/atlassian-jira-software-8.13.0-EAP03.tar.gz'
          md5: 'd9b114b07ec393c8ca681303373a0d69'
          tags: '8.13.0-EAP03 8.13'
        - application: jira-software
          version: '8.12.2'
          url: 'https://www.atlassian.com/software/jira/downloads/binary/atlassian-jira-software-8.12.2.tar.gz'
          md5: '357038312aa7fb791bdd9e30d1982c28'
          tags: '8.12.2 8.12 8'
        - application: jira-software
          version: '8.11.1'
          url: 'https://www.atlassian.com/software/jira/downloads/binary/atlassian-jira-software-8.11.1.tar.gz'
          md5: '877967ac46c974bd4c3dbabd1e0fb775'
          tags: '8.11.1 8.11'
        - application: jira-software
          version: '8.10.1'
          url: 'https://www.atlassian.com/software/jira/downloads/binary/atlassian-jira-software-8.10.1.tar.gz'
          md5: 'b8305b7298c8a8f1c9ea0f0d1050d17e'
          tags: '8.10.1 8.10'
        - application: jira-software
          version: '8.9.1'
          url: 'https://www.atlassian.com/software/jira/downloads/binary/atlassian-jira-software-8.9.1.tar.gz'
          md5: '4af366c5701be5b19bd036975a085348'
          tags: '8.9.1 8.9'
        - application: confluence
          version: '7.8.0-beta1'
          url: 'https://www.atlassian.com/software/confluence/downloads/binary/atlassian-confluence-7.8.0-beta1.tar.gz'
          md5: 'b6ae2b1d7ba605d15f585cf31870562b'
          tags: '7.8.0-beta1 7.8'
        - application: confluence
          version: '7.7.3'
          url: 'https://www.atlassian.com/software/confluence/downloads/binary/atlassian-confluence-7.7.3.tar.gz'
          md5: '385fbeeab587ff918eb9c09039334b45'
          tags: '7.7.3 7.7 7'
        - application: confluence
          version: '7.6.2'
          url: 'https://www.atlassian.com/software/confluence/downloads/binary/atlassian-confluence-7.6.2.tar.gz'
          md5: '7d72a1bd874bc2fb769efdce0a376c39'
          tags: '7.6.2 7.6'
        - application: confluence
          version: '7.5.2'
          url: 'https://www.atlassian.com/software/confluence/downloads/binary/atlassian-confluence-7.5.2.tar.gz'
          md5: '3b5f6daa4dc6de0e45eddb0ed7b71611'
          tags: '7.5.2 7.5'
        - application: confluence
          version: '7.4.4'
          url: 'https://www.atlassian.com/software/confluence/downloads/binary/atlassian-confluence-7.4.4.tar.gz'
          md5: '91bcd385aaa06b95703f4a1b39f7598b'
          tags: '7.4.4 7.4'
# DONT-CHECKSUM-END
    env:
      IMAGE_PATH: ${{ secrets.IMAGE_PATH }}${{ matrix.application }}
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Calculate checksum
      env:
        MD5: ${{ matrix.md5 }}
        FILES: |-
          .github/workflows/build.yml
          docker/**
          docker-shared-components/support/**
          application/${{ matrix.application }}/patch.diff
      run: |
        FILES_HASH=$(./hashFiles.py <<<${FILES})
        echo "::set-env name=CHECKSUM::${MD5}-${FILES_HASH}"
    - name: Fetch last checksum from Docker image label
      env:
        TAG: ${{ matrix.version }}
      run: |
        set -o pipefail
        (set +e; skopeo inspect "docker://${IMAGE_PATH}:${TAG}"; :) | ./last_checksum.py
    - name: Download from cache
      if: env.CHECKSUM != env.LAST_CHECKSUM
      uses: actions/cache@v2
      id: cache
      with:
        path: app.tar.gz
        key: ${{ matrix.application }}-${{ matrix.md5 }}
    - name: Download from Atlassian
      if: env.CHECKSUM != env.LAST_CHECKSUM && steps.cache.outputs.cache-hit != 'true'
      env:
        URL: ${{ matrix.url }}
      run: |
        wget --quiet --show-progress --progress=dot:giga --output-document app.tar.gz "${URL}"
    - name: Check integrity
      if: env.CHECKSUM != env.LAST_CHECKSUM
      env:
        MD5: ${{ matrix.md5 }}
      run: |
        set -o pipefail
        echo "${MD5}  app.tar.gz" | md5sum --check
    - name: Extract
      if: env.CHECKSUM != env.LAST_CHECKSUM
      run: |
        mkdir docker/app
        tar -xf app.tar.gz -C docker/app --strip-components=1 --no-same-owner
    - name: Patch
      if: env.CHECKSUM != env.LAST_CHECKSUM
      working-directory: docker/app
      run: |
        shopt -s globstar nullglob
        patch -p1 <../../application/${{ matrix.application }}/patch.diff
        mkdir -p conf/Catalina/localhost
        rm bin/**/*.bat bin/**/*.{dll,exe}{,.x64} temp/safeToDelete.tmp
    - name: Copy support scripts
      if: env.CHECKSUM != env.LAST_CHECKSUM
      run: |
        cp -lr docker-shared-components/support docker/
    - name: Copy entrypoint.sh
      if: env.CHECKSUM != env.LAST_CHECKSUM
      run: |
        cp -l application/${{ matrix.application }}/entrypoint.sh docker/
    - name: Enable dockerd --experimental for docker build --squash
      if: env.CHECKSUM != env.LAST_CHECKSUM
      run: |
        DOCKERD_CONFIG=$(jq '.+{experimental:true}' /etc/docker/daemon.json)
        sudo tee /etc/docker/daemon.json <<< "${DOCKERD_CONFIG}"
        sudo systemctl restart docker
    - name: Build Docker image
      if: env.CHECKSUM != env.LAST_CHECKSUM
      run: |
        . application/${{ matrix.application }}/build-env.sh
        APP=${app^^}
        IMAGE_ID=$(docker build --squash --quiet --label "build.checksum=${CHECKSUM}" --build-arg "APP=${APP}" --build-arg "USER=${app}" --build-arg "GROUP=${app}" --build-arg "UID_GID=${UID_GID}" --build-arg "PORT=${PORT}" --build-arg "APP_INSTALL_DIR=/opt/atlassian/${app}" --build-arg "APP_HOME=/var/atlassian/application-data/${app}" docker)
        echo "::set-env name=IMAGE_ID::${IMAGE_ID}"
    - name: Push to Docker Hub
      if: env.CHECKSUM != env.LAST_CHECKSUM && github.ref == 'refs/heads/master'
      env:
        TAGS: ${{ matrix.tags }}
        REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
        REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
      run: |
        docker login --username "${REGISTRY_USERNAME}" --password "${REGISTRY_PASSWORD}" "${IMAGE_PATH%%/*}"
        for TAG in ${TAGS}; do
        	docker tag "${IMAGE_ID}" "${IMAGE_PATH}:${TAG}"
        	docker push "${IMAGE_PATH}:${TAG}"
        done
    - name: Save Docker image
      if: env.CHECKSUM != env.LAST_CHECKSUM && github.ref != 'refs/heads/master'
      run: |
        docker save -o docker-image.tar "${IMAGE_ID}"
    - name: Upload Docker image as artifact
      if: env.CHECKSUM != env.LAST_CHECKSUM && github.ref != 'refs/heads/master'
      uses: actions/upload-artifact@v2
      with:
        name: ${{ matrix.application }}_${{ matrix.version }}
        path: docker-image.tar
